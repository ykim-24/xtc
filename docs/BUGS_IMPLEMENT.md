### Implement

- After doing a review user the comments being applied should also suggest a fix
- When doing a self review we should have a fix button that will make the changes after evaluating if it is valid

### Bugs

- After starting work for a work tree once it finishes going back to linear it has the start work modal but empty
- Unable to run test in the work tree due to npx not found
- .env are not being picked up properly in worktrees
- Failed to read: rules.json
